import React from 'react';
import ReactDOM from 'react-dom';
import BaseReact from './client/baseReactFile';

ReactDOM.render(
  React.createElement(
    BaseReact,
  ),
  document.getElementById('react-base-mount')
);
